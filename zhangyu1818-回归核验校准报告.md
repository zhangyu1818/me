# 校准报告：zhangyu1818 博客重新核分

## 1) 分数是否系统性偏差

| 指标 | 旧版(era-aware) | 新版(回归原文) | 变化 |
|---|---|---|---|
| 深度均值 | 2.52 | 2.51 | 基本持平 (-0.01) |
| 增量均值 | 2.40 | 2.45 | 略升 (+0.05) |
| "当时即错"条数 | 146 / 涉 62 篇 | 82 / 涉 37 篇 | **-44%，-25 篇** |
| 深度 4 分篇数 | 12 | 11 | 持平 |

**结论：分项均值基本一致（偏差在 ±0.05 量级，可视为无系统性偏差），但"错误认定"环节存在显著的系统性偏严。**

- **均值层面无系统偏差**：两轮深度/增量均值几乎重合，说明分档尺度本身是稳的。增量分布上 3 分篇数由 26 升至 31，反映本轮把若干"被当平庸"的实战/源码文（如 #84、#88、#118）上调了半档，方向正确但幅度温和。
- **错误认定层面系统性偏严**：旧版抽取 146 条"当时即错"，本轮逐字回归原文后仅 82 条成立，撤回率约 44%，涉及篇数从 62 降到 37。**原因有三**：(a) 把拼写/输入法笔误（source-mao、webpackChunksName、Safafi、redux-sage、stuct 等）当作技术硬伤；(b) 把"作者没深挖/跟随主流插件"误判为"讲错"（典型 #86、#94、#121）；(c) 把作者诚实标注的"我没想透/可能不对"当扣分项（#77、#106、#116）。这正是题设警示的三类偏严。

---

## 2) 真实错误清单（权威数字：82 条，替代旧 146 条）

按严重度归类（全部以逐字原文证实，疑罪从无）：

| 严重度 | 计数 |
|---|---|
| 高 | **0** |
| 中 | **5** |
| 措辞不准 | **6** |
| 笔误 | **71** |
| 合计 | **82**（涉 37/67 篇）|

**"高"级：无。** 全部 67 篇中不存在写作当时即错且动摇主体结论的硬伤。

**"中"级（5 条，逐条带文章号）：**

- **#67《swift 5.1 入门学习》**：`...`（闭区间）小节代码仍写成 `for i in 0..<4`，与标题"包含最后一项"及注释 `[0,1,2,3,4]` 自相矛盾，应为 `0...4`。代码与所述语义不符。
- **#69《RxJS（二）创建类操作符》**：伪数组（键 0/1/2，值 0/1/2）经 `from` 输出应为 `0 1 2`，注释写成 `1 2 3`，与给定输入不符。
- **#81《Less（一）基本语法》**：称阻止 mixin 编入 CSS 的方式是"加空格"，实际是名字后加括号 `()`；被作者自己紧接的示例（`.my-other-mixin()` 不输出 / `.my-mixin` 输出）证伪。
- **#97《qiankun 微前端 demo》**：示例只 `import registerMicroApps`，末尾却调用 `start()`，`start` 未导入，照抄报 `start is not defined`（示例代码遗漏）。
- **#117《React Diff 算法》**：把 Placement 的 `insertBefore` 时机误记为"Layout 阶段"，实际发生在 commit 的 mutation 子阶段；React 17 源码即如此。单句事实错误，不动摇整体算法讲解。

**"措辞不准"（6 条，仅计数+定位）：** #65（Omit 语义语序颠倒）、#85（commonJS/ES 模块表述含糊）、#89（声称定义 3 个 mixin 实际只给 1 个）、#97（plugin-umi 命名错 + "普通 qiankun" 小节误放 umijs 写法，共 2 条）、#126（async/sync prose 与代码矛盾）。

**"笔误"（71 条，仅计数）：** 拼写/输入法/复制粘贴/脱字/标点类，分布在 #64、#65、#67、#69、#70、#72、#73、#74、#75、#76、#77、#78、#80、#83、#85、#86、#87、#88、#92、#94、#95、#99、#101、#102(0)、#106、#107、#110、#112、#118、#120、#122、#123、#126、#127 等篇，均不影响技术正确性，按铁律不扣深度分。

---

## 3) 被低估的硬核 / 确属平庸

**被低估的硬核（应给予正面/上调评价）：**

- **React 17 源码精读群（2021 年发布数月内、中文社区稀缺）**：#77（react-redux v7 Subscription 嵌套订阅）、#107（Mount Fiber 树创建）、#108（useState/useReducer 环形链表手推）、#109（useEffect/useLayoutEffect 各 commit 阶段）、#110（类组件全生命周期）、#111（Context valueStack 出入栈）、#112（未文档化 calculateChangedBits 第二参数）、#114（render-phase update 源码级解释 + useRef 原创封装）、#115（EffectList 冒泡收集）、#116（事件机制冒泡/捕获 + stopPropagation 原理）、#117（Diff，含 1 处中级硬伤但主体准确）、#120（props 导致更新的 Fiber 级讲透）。
- **Vue3 响应式（2020-09 发布一月内）**：#105（Proxy 响应式极简实现）、#106（Proxy 拦截 Map/Set 的 this 死循环 + `[[MapData]]` 内部槽）、#122（@vue/reactivity 逐行精读，贴当时真实函数式 effect 源码）。
- **实战工程一手沉淀**：#84（JsSIP 四种取流方案实测兜底 + 补 TS 类型，中上）、#102（SwiftUI 逐机型截图测量小组件像素表）、#137（Swift 跑 Silero VAD，命中全部 ONNX 接口约束）、#86（Flutter 20+ 页面踩坑解法）、#88（Hooks 数据流"手动—半自动—全自动"综述）、#124（fastlane RN 双端签名注入）。
- **小而硬**：#70（RxJS 合并类时序逐一跑对，含 `[1,0,3]` 精确推演）、#118（GitHub 当数据库完整链路）。

**确属平庸转述（低分公允，但不应因笔误重罚）：** #65（TS 工具泛型）、#66（Trojan 搭建）、#67（swift 入门）、#68–#69、#72–#73、#79–#80、#81–#83、#85（Jest）、#87（CRA v3）、#95–#96（年终随笔/读后感，体裁使然）、#99–#101（读书笔记）、#103（换博客随笔）、#121（单测入门）、#135（渲染演示页）。这些深度/增量低是题材定位与体裁决定，而非"讲错"。

---

## 4) 对作者评价的净修正

- **技术水平：维持中上，局部上修。** 2020–2021 的 React 17 / Vue3 源码群（#107–#122）是发布数月内的稀缺硬核，证明作者具备一手通读主流框架源码并自洽推演的能力，非"跟随主流"可概括。
- **严谨度：明确上修。** 旧版 146 条"错误"经逐字核验仅 82 条成立、且 0 高、仅 5 中、71 条为笔误；作者多处主动标注"没想透/可能不对"是诚实而非缺陷，先前偏严打分应予纠正。
- **内容增量：维持中位、个别上修。** 大量学习/读书笔记确属诚实但低增量的转述（增量 1–2 合理），但实战与源码文的原创取舍、踩坑封装被低估，增量均值由 2.40 升至 2.45 方向正确。

**一句话定调：均分尺度无需大改，但"错误"权威数字应由 146 条订正为 82 条（0 高 / 5 中 / 6 措辞 / 71 笔误），作者的严谨度与源码硬核能力此前被系统性低估，本轮应作净上修。**
