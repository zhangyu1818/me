# zhangyu1818 博客作者画像与综合评估报告（以写作当时为基准）

## 评判方法论（务必先读）

本报告的全部判断，一律以作者**写作当时**的技术水位为基准，坚决反对"事后诸葛"式的事后评判。具体执行三条铁律：

1. **区分两类"问题"。** (A) **当时即错**——在文章写作的那个时间点，按当时通行的知识与工具就已经是错误或会坑读者的，计为真问题，可批评；(B) **当时正确/合理、如今过时**——写作时是恰当选择，只是被技术演进自然淘汰的，计为**自然折旧，绝不算作者过错**。例如 2019 年讲 webpack 4/CRA、2020 年因某 CSS 特性彼时支持有限而做 SCSS fallback 降级，都是当时审慎的工程实践，不因"今天有了更好的"而扣分。这等同于"不能因为某人在 jQuery 时代没用 React 就说他没有现代化思想"。

2. **难度与稀缺度计入正面证据。** 在某项技术发布后极短窗口期内就吃透并产出（如 Vue3 发布一月内手写 Proxy 响应式、React 17 发布数月内系统通读源码），其"当时做此事的难度与稀缺度"作为正面加分，而非以"今天这些都是常识"来抹平。

3. **"内容增量度"与著作权无关。** 本报告多处评估"原创增量"（即作者在官方文档/参考书之上贡献了多少自己的踩坑、封装、对比、提炼），这**仅是内容价值维度的衡量，与著作权、抄袭与否完全无关**。需明确：作者全部内容均为**本人原创撰写，无搬运转载**；"增量低"指的是"忠实转述型笔记的个人增量有限"，绝非质疑其原创性。

本报告已先行采纳质疑者的校准要点（尤其时代错置审查结果）后再行文：包括去除对 #121 选 RTL 的"前瞻性"拔高（实为合理跟随主流）、统一"146 条当时即错"仅下压"公开写作严谨度"子维度而非整体定级、将"源码高级"限定为"同步渲染主线"、把 #118 升级为 OAuth 架构选型错误、重排 A 类危害（定义写反 > 忘 restart）等。

---

## 一、速览 TL;DR

- **核心护城河是 React/响应式源码的逐行钻研。** 这是全部 67 篇里最硬、最具稀缺度的部分，且评分集中在最高档——能力上限由此确立。
- **技术嗅觉长期在线，几乎从不滞后。** GraphQL（2019）、Vue3 响应式（2020 发布一月内）、React 17 源码（发布数月内）、Yarn2、App Router/RSC（2024）、端侧 AI/VAD（2025），稳定走在中文社区主流前沿，**绝非滞后者**。
- **"诚实"是贯穿 67 篇的人格底色。** 反复主动标注"没看懂/存疑/省略"，不装专家——这是其可信度的基石，也是技术博主中稀缺的品质。
- **最稳定的短板有两条：可避免的笔误成灾，与"停在 how、不深挖 why"。** 前者靠发布纪律即可消灭，后者是卡住深度上限的真正瓶颈。
- **源码深度存在系统性天花板：几乎全部主动回避 Lanes/Scheduler/优先级。** 即 React 并发架构最硬的约 30%，覆盖了易讲的主线、绕开了最难的部分。
- **能力轨迹呈 U 型 + 技术栈持续下沉**：前端纵深 → 跨端 → 原生 Swift/iOS → 端侧 AI。深度回落不等于退步，而是"广度扩张期"每进新领域从浅层重启的自然现象。
- **近年已是 AI 辅助开发的常规使用者**（#137 坦承代码由 Claude 生成），并开始英文写作，可能面向更国际化的读者或环境。

**一句话定级：一个以 React/响应式源码为真正护城河、靠"诚实记录学习过程"持续产出的成长型工程师——中级偏上 / 接近高级（源码同步主线维度可达高级），技术嗅觉敏锐、动手实证强、自我认知清醒，但精度不足、深度偶止于表面。**

---

## 二、作者画像

### 技术栈与专长

**主轴是 React 及其生态，且达到了源码级理解**，这是全部 67 篇中最硬、最有分量的部分。从 2020 年初读 Redux 源码起步（createStore/applyMiddleware/compose），2020 年 3 月即啃下当时刚用 hooks 重写、实现极绕的 react-redux v7（被评为"走在社区前沿"），到 2021 年初密集产出一整套 React 17 源码解析——mount 流程、Hooks、Effect、类组件、Context、useMemo/useCallback、EffectList、事件系统、diff、PureComponent/memo、props 更新机制。这批文章技术深度普遍是全样本最高的一档，且都基于"当时最新稳定版 17.0.1"通读源码，选题踩在社区热点（卡颂源码课正热）上，讲解质量靠前。

**React 之外的几条副线，按热爱程度递减排列：**

- **RxJS**：7 篇系列学习笔记，从 Observable 生命周期到各类操作符，2018–2020 跟着《深入浅出 RxJS》系统补课。他在年度总结里直言"对 RxJS 着迷却难落地实践"——真心喜欢但没找到落地场景的技术。
- **CSS**：6 篇，含《CSS 世界》《CSS 选择器世界》读书笔记与 prefers-color-scheme 暗色模式实测。其中做了真实的 Chrome vs Safari 跨浏览器实测，不止抄文档。
- **TypeScript 类型体操**：内置工具类型、infer（结合 React 源码与实战讲解）。
- **构建工程化**：webpack 4、CRA v3、qiankun 微前端、Yarn 2 Berry monorepo（2021.08 较早的中文实践）。
- **跨端/移动**：Flutter（2019 下半年公司项目实打实交付一个 App）、Swift/iOS（入门 → WidgetKit 透明小组件并上架 App Store → Swift Concurrency → 端侧 VAD）、React Native 原生模块（Swift+Kotlin 双端）与 fastlane CI。

**专长画像**：他不是广度型的"什么都会一点"，而是**在 React 上扎得很深、在其余领域做扎实的主流跟随**。源码阅读是他真正的核心竞争力。

### 职业阶段与角色推断

**典型的成长型中级前端工程师，全程可见清晰的能力爬升曲线。**

- **2018–2019（入门/夯基期）**：webpack 4 速查、Swift 入门照搬官方 Tour、运维杂记，内容多为文档转述、深度偏低。
- **2019–2020（系统补课期）**：RxJS 七连发，读《你不知道的 JS》《CSS 世界》《图解 HTTP》《深入浅出 RxJS》（年度总结自陈读完 4 本书），Flutter 交付公司项目，GraphQL 上手 Apollo 全家桶（2019 初属较前沿）。
- **2020–2021（源码进阶期，能力质变）**：从 Redux 源码切入，2021 年初爆发式产出 React 17 源码全家桶。年度 flag 立的"深入 React 源码与事件机制"在一年后被一篇篇兑现——**说到做到，是这个画像里最可信的一点**。
- **2021 末–至今（横向扩张期）**：转向 RN、Swift/iOS 上架应用、monorepo 工程化，并把博客作为持续的练兵场。

**角色信号**：长期在业务驱动下工作——WebRTC SIP 软电话、Flutter App、IM 多消息类型重构、qiankun 微前端，都明确交代了真实业务动机。读《浪潮之巅》一篇里他自评"长期按产品经理要求做、缺乏独立思考，停留在工程师第五等"，并记录"辞职一个多月、疫情可能影响找工作"（约 2020 初）——说明他经历过求职波动，且对自己的执行者角色有清醒认知。**英文写作的出现（2024 年若干 EN 篇）暗示后期可能进入更国际化的工作环境或主动面向英文读者。**

### 学习与产出动机

1. **博客是个人学习的副产品与"第二大脑"**：绝大多数是学习笔记/源码解析（源码解析 21 篇、学习笔记 17 篇），多为"边学边记"。他对内容价值诚实自评"不拔高不注水"，并反思读书"获取的多是思想难落地"。
2. **工作痛点驱动**：多篇明确交代真实业务动机，"不是为写而写"。
3. **博客工程本身是反复折腾的对象**：Gatsby → GitHub Issues 当后端 → Next.js App Router + GitHub Discussions，博客被重写多次，既是练手项目也是技术品味的展示橱窗。
4. **造轮子的冲动**：60 行动画库致敬 animateplus、手写 Vue3 响应式、cjsb 打包工具、useDerivedState 封装。

### 性格与工作方式

**最突出的特质是"诚实"——这是贯穿 67 篇的人格底色。** 反复主动标注"没看懂/没搞明白/个人见解可能不对"（updateQueue"没搞明白"、优先级判断"没看懂"直接略过、"逻辑狗屁不通"自评、this 方案标"可能不一定对"、SIP 事件"我现在也不是特别懂"）。**不装懂，这在技术博主中很可贵。** 自我批评毫不留情："上半年停滞 4 个月、早起计划失败、轮子很菜"；自评"第五等工程师"。

工作方式特点：**重实证**（RxJS 笔记几乎每个示例都"运行→发现问题→改代码→再观察"，做真实跨浏览器实测）；**善类比降门槛**（Observable 比作数组、操作符比作数组方法、redux connect 类比高阶函数、洋葱模型"平铺展开闭包"、圣诞树彩灯比 EffectList）。**典型短板是校对不细、笔误成灾**——几乎每篇都有拼写/术语错误，这是他最稳定的弱点，不影响主旨但损害可信度；以及深度时常受限于"贴源码+注释"，对"为什么这么设计"展开不足。

### 兴趣迁移轨迹

**前端深耕（React/RxJS/CSS）→ 跨端探索（Flutter/RN）→ 原生 Swift/iOS → AI 辅助端侧应用**，是一条清晰的"从 Web 向外扩张"的轨迹。2018–2021 是纯前端纵深；2019 起在 SwiftUI 刚公布时就主动关注 Swift；2021 末转向移动端工程化；2020 末起对 Swift/iOS 越投入越深（"自从 M1 出了我就彻底变果粉了"，WidgetKit 小组件上架 App Store）；2024–2025 拥抱 AI 辅助与端侧 AI（在 iOS 端用 ONNX Runtime 跑 Silero VAD，**并坦承"整段代码由 Claude 生成"**）。

---

## 三、技术水平评级（以写作当时为基准）

### 分维度评级表

| 维度 | 等级 | 当时基准的证据 | 短板 |
|---|---|---|---|
| **React/前端框架源码理解** | **高（限同步渲染主线）** | 14 篇 React 源码 + 1 篇 Redux + 2 篇 Vue/响应式，是最厚的一摞。2020.03 啃 react-redux v7 hooks 版（逐层订阅链、ref 而非 state 存中间值的原因）；2021 初连续通读 React 17 源码产出 mount/Hooks/effect/类组件/Context/EffectList/事件系统/diff 全链路（深度多为 4）；#120 把"diff 不决定更新、只决定 Fiber 复用、props 用 `!==` 判断"这一核心认知纠偏讲透，呼应 Dan 同期《Before You memo()》；#112 用并位运算讲清 calculateChangedBits/observedBits 这一几乎无中文资料的隐藏实验 API。当时（2021 初）系统通读最新版源码并产出，稀缺度与难度都高。 | **几乎所有源码篇都主动"省略优先级/Lanes/Scheduler"——并发架构最硬部分系统性留白，故"高级"仅限同步渲染主线**；多停留在"带注释的源码走读"，how 多 why 少；commit 阶段 Placement 在 mutation 而非 layout、O(n³)→O(n) 归因等表述硬伤反复出现，精度不足。 |
| **React 工程应用** | **中** | #88 在 Hooks 满一年时横向归纳数据流方案并提"手动—半自动—全自动"演进框架，有个人提炼；#92 柯里化解耦 IM 消息创建/发送、#93 Formik+yup 选型对比、#84 前端 SIP 软电话（WebRTC 冷门垂直场景，中文踩坑稀缺）都贴真实业务。 | 多数停留在主流工具标准用法 + 官方文档转述；示例代码常有命名冲突/未定义引用等可运行性 bug（#92 sendMessage 与 createMessage 混用）；性能优化"基本没考虑过"（作者自陈）。 |
| **TS 类型系统** | **中** | #94 infer 解包（数组/Promise/Ref）结合 @types/react useReducer 真实源码讲解（深度 3）；#65 通读 lib.es5.d.ts 工具类型。 | 仅 2 篇主攻，**样本偏少**；#65 对 Omit/Extract 文字定义写反、未讲条件类型分发特性；原理（分发、为何只能在 extends true 分支）讲得浅。 |
| **状态管理与响应式原理** | **高** | Redux 源码（createStore/compose/applyMiddleware 洋葱模型）；**2020.10 Vue3 发布仅一月即手写 Proxy 响应式**（#105 依赖收集压栈取栈顶的 trick 讲得最透）、并追到 Map/Set 的内部槽 [[MapData]] 报错根因（#106）；#122 通读 @vue/reactivity 源码（targetMap 三层嵌套、数组防循环依赖 PR）。当时手写 Proxy 响应式 + 追集合类型特殊处理属紧跟前沿。 | 极简版常只给方向不落实现；effect 嵌套/cleanup/scheduler 等真实系统关键问题深度止于入门拆解；#106 的 .bind(target) 方案对非函数属性会抛错（作者已标注存疑）。 |
| **CSS** | **中** | 6 篇。#100《CSS 世界》读书笔记抓住盒子内外/line-height/幽灵空白节点/margin 合并/BFC 等最难点（深度 3）；#99 系统梳理 :focus-visible/:is()/:where() 等当时前沿伪类并标注支持现状；#89 在 prefers-color-scheme 刚落地即整合 CSS 变量 + SCSS fallback 成可落地链路（增量 3，**fallback 是当时的工程审慎，不计过错**）；#78 实测 Chrome vs Safari 的 MediaQueryList 差异。 | 笔误偏多（zh-Hnat、--bg 用在 color）；多为读书笔记/文档梳理，原创验证较少；选择器性能、:is() 优先级精确规则未深入。 |
| **工程化/构建/Monorepo** | **中** | webpack4 体系梳理（含 VLQ 编码原理）、CRA v3 新特性；**2021.08 较早写 Yarn2(Berry) 中文实践**（真实仓库可复现，踩坑 PnP 切回 node-modules、自写 cjsb 按序打包）、qiankun 微前端、fastlane 双端自动化。选题常踩在前沿。 | 普遍"文档梳理为主"，核心机制（PnP 原理、幽灵依赖、tapable/loader-plugin 底层）几乎不展开；示例代码有可运行性问题；错别字多。 |
| **测试** | **中（仅记录涉猎，不参与定级）** | 2 篇：Jest 入门（区分 toBe/toEqual 底层 Object.is vs 递归）、RTL 选型。**注：2021 中选 RTL 符合当时社区主流（官方推荐 + CRA 内置），属合理跟随，非前瞻性判断。** | 样本 ≤2。停留在官方文档 API 罗列；#121 Hooks 用例用 waitFor 触发 increment 而非 act（当时即不规范）；RTL 核心理念、queries 优先级未讲。 |
| **跨端（RN/Flutter/Swift-iOS）** | **中** | 11 篇，跨度 2019–2025。Flutter 真实项目交付 + 系统复盘（widget 重建/焦点/保活理解到位）；RN 原生模块 Swift+Kotlin 双端实战（填补中文空白）；Swift Concurrency 较早整理；**2025 端侧：ONNX Runtime+CoreML 跑 Silero VAD**（双阈值状态机 + RNN 隐状态维护正确）。广度好、敢啃新东西、贴真实交付。 | 概念措辞不严谨（await 说成"阻塞"、TimelineProvider 协议说成"继承"）；示例代码复制粘贴遗留与笔误密集（#126 多处取错 key、.sync 主线程死锁风险未提示）；性能优化常停在现象层、无实测数据（#137 声称用 NeuralEngine 仅凭日志）。**注：新领域入门期的概念失误（如 await）量级轻于自称深耕的 React 源码篇的精度问题。** |
| **计算机基础/算法** | **中（偏记录，样本薄）** | #101《你不知道的 JS》抓住 [[Put]] 屏蔽分支/不变性方法/this 四规则等硬核细节（深度 3）；RxJS 7 篇体现函数式/响应式思维；source-map VLQ 编码、NAT/STUN/TURN 等原理有涉及。 | 无专门算法/数据结构内容；计算机基础多通过读书笔记和源码间接体现，非系统训练。 |
| **系统与 AI 集成** | **中（偏前沿尝鲜，样本薄）** | 运维 4 篇（trojan/proxychains/SSH 加固/Seafile，主流跟随、实用）；**2025 端侧 AI 集成（ONNX/CoreML/VAD）**与博客重写（RSC/MDX/shiki/GitHub Discussions 当后端）体现对 AI 辅助与端侧推理的及时跟进。 | 运维篇普遍"零原理、照抄脚本"（**程度同当时同类内容持平，属写作风格短板而非'不懂原理'**）；AI 集成篇近乎纯代码堆砌、缺机制解释与实测验证；系统底层无深入。 |

### 综合定级

**级别：中级偏上 / 接近高级前端工程师（源码理解在同步渲染主线维度可达高级，并发/调度维度仍是空白）。对标大厂职级约 P5+ ~ 接近 P6 / L4 偏上（不及 L5/资深）。**

**置信度：中等（约 65%）。**

**支撑"偏上"的核心证据：**

- **源码阅读是真功夫，且有时间稀缺度加成。** 2021 年初连续、系统通读 React 17 源码并产出 9 篇覆盖 mount/Hooks/effect/类组件/Context/diff/事件系统/EffectList 的解析，2020.10 在 Vue3 发布一月内手写 Proxy 响应式并追到 Map/Set 内部槽——这些在写作当时是"多数人用而不究其原理"的硬核动作。**注意：深度均值虽仅 2.52，但该均值被大量学习笔记/转述拉低；定级主要依据是 4 分段的能力上限（12 篇 4 分几乎全在源码维度）。**
- **认知纠偏能力。** #120（diff 不决定更新）、#112（隐藏 API）、#88（数据流演进框架）显示他不只是抄，而是能提炼出有传播价值的心智模型，并与社区前沿（Dan 的文章）同步。
- **技术嗅觉与广度。** 从 webpack4→Yarn2、CRA→Next.js/RSC、Flutter→RN→Swift Concurrency→端侧 ONNX VAD，长期紧贴甚至略微领先社区前沿，且敢在真实业务/个人项目落地。
- **职业态度健康。** 几乎每篇都诚实标注"没懂/存疑/省略"，不装专家，这是可信工程师的特质。

**拉低到"不及资深"的证据：**

- **深度有系统性天花板。** 源码篇几乎全部主动回避 Lanes/Scheduler/优先级——而这恰是 React 并发架构最硬、最有区分度的部分。"读懂别人写的"与"能设计/推导为何这样设计"之间仍有差距。
- **公开写作的严谨度偏弱。** 146 条"当时即错"涉及 62/67 篇，大量是命名冲突、未定义引用、概念混淆、可运行性 bug。**此项证据仅下压"公开技术写作严谨度"这一子维度，不等比例下压整体工程能力定级**——因为博客常是"边学边记"，其 bug 率未必等于生产代码质量（见下方诚实边界）。
- **原创增量整体偏低。** 增量度均值 2.40，3 分及以上仅 29 篇，大量篇章是官方文档/参考书的忠实转述，"独立研究"成分有限（**此为内容价值维度，与著作权无关，全部内容均为原创撰写**）。

**诚实边界：** 博客样本**只能反映自学能力、源码钻研、技术嗅觉、表达**这几个维度，**无法覆盖**：系统设计（看不到大型架构与技术选型决策）、技术决策与权衡（缺乏在团队/工期/历史包袱约束下做权衡的证据）、协作/影响力/工程规范（无法评估 code review、跨团队推动、带人）、生产级工程质量（博客示例 bug 率偏高，但这未必等于生产代码质量）。因此真实职级可能上浮或下沉，故置信度仅中等。

### 真本事 vs 看起来深（以当时为基准）

**判定为"真本事"的部分：**

- **React 源码理解（同步主线）** —— 不是看起来深，是真的逐行读过最新版源码、能串起跨文件调用链、能抓住别人忽略的细节（函数组件 fiberTag 默认 IndeterminateComponent、原生标签直接文本子节点少建一个 fiber、ref 而非 state 存中间值的原因）。这种细节只有真读过才写得出。
- **响应式原理** —— 手写 Proxy 依赖收集（压栈取栈顶 trick）、追 Map/Set 的 [[MapData]] 内部槽报错根因，是从机制层面理解，而非 API 复述。
- **认知纠偏** —— #120/#112 这类"纠正流行误解 + 落到源码字段佐证"的内容，是消化后的理解，不是搬运。
- **技术嗅觉** —— 长期、稳定地踩在前沿（2020 Vue3 一月内、2021 React17 半年内、2025 端侧 AI），这是真实的学习驱动力。

**判定为"看起来深、实则有限"的部分：**

- **被"省略优先级/调度"撑起的深度幻觉。** 多篇源码解析评分 4，但几乎都在最关键的 Lanes/Scheduler 处写"省略/没看懂"。读者会觉得"讲得很深"，但实际上**绕开了最难的约 30%**——覆盖了易讲的主线，回避了真正硬的调度机制。
- **大量"文档转述型"笔记的伪深度。** RxJS 7 篇、Less/Jest/工具类型等学习笔记，信息密度看着饱满，但增量低、原理浅，本质是高质量的官方文档中文化（**原创撰写、非搬运，但个人增量有限**）。
- **运维/教程类的零原理实用主义。** trojan/proxychains/SSH 等能跑通、实用，但完全不讲原理——属于"能用"而非"懂"。
- **AI 集成/前沿选型的卖点式罗列。** 博客重写与端侧 VAD 选型前沿、看着先进，但缺机制解释、缺实测验证（#137 声称走 Neural Engine 仅凭日志，而 CoreML EP 对含 RNN 的模型常无法整图加速、易回退——**这一限制在 2025 年中已是 ONNX Runtime 文档的已知行为，故"未验证"属当时即可指出的真问题**）。

**一句话总结：** 他的"真本事"集中在 **React/响应式源码的逐行钻研与认知提炼**——这部分扎实、有稀缺度、值得信任；而"看起来深"主要来自**系统性回避调度/优先级这类最硬骨头**，以及**大量学习笔记的文档转述**。是一个**自学能力强、嗅觉敏锐、源码功底扎实但精度和系统纵深尚未到资深级**的中级偏上工程师。

---

## 四、技术成长曲线（以写作当时为基准）

> 日期标注 **[真]**=掘金/正文锚定，**[推]**=据技术纪元推断。

### 阶段 0｜2018 下–2019 中 [推]：工程化萌芽与"基本功补课"

**关注点**：webpack 4 体系、Babel 7、source-map 原理、CSS 预处理器（Less）、Jest 入门、CRA v3，以及运维科学上网（trojan、proxychains）。

**代表作**：webpack4 全配置梳理（深度 2/增量 2）——覆盖面完整、附 VLQ 编码原理，但有 `webpackChunksName` 拼写等当时即错的硬伤。

**相对当时潮流的位置**：**主流跟随**。webpack4 是 2018–2019 的绝对主流，作者紧贴官方文档系统化梳理，既不超前也不滞后。技术纪元标记（corejs:2 而非 3、@babel/ 命名空间、cheap-module-eval-source-map）都精准卡在当时。

**心态**：学习者姿态，靠官方文档"复述 + 逐字段中文注释"，已显露"知其所以然"的意识（追问 Tree Shaking 为何只支持 ESM）。

### 阶段 1｜2019 初–2020 中 [真/推混合]：响应式编程长征 + 前端工具/模式爆发

博客密度最高、最能体现"系统性学习习惯"的阶段。三条线：

1. **RxJS 七连**（[推]2018 下–2020）：从 Observable 生命周期到回压，几乎通刷官方操作符体系。
2. **GraphQL/Apollo**（[真]2019-04）：在中文资料稀缺时即上手 Apollo 全家桶 + Koa，端到端可跑。
3. **应用与模式**：Flutter 落地公司项目（[推]2019Q4）、WebRTC/JsSIP 软电话（[真]2020-04）、qiankun（[真]2020-06）、策略模式 + 柯里化（[真]2020-05）、Formik（[真]2020-07）、Hooks 数据流横评（[真]2020-04）。
4. **读书笔记**：《你不知道的 JS》《CSS 世界》《CSS 选择器世界》《浪潮之巅》。

**深度/增量变化**：RxJS 稳定在深度 2/增量 2；应用类明显跃升——WebRTC、Flutter、数据流均达深度 3/增量 3，开始有真实业务踩坑与个人提炼。

**相对当时潮流的位置**：RxJS、Less、Jest = **主流跟随**；GraphQL/Apollo、prefers-color-scheme 暗色模式、Hooks 数据流、WebRTC SIP = **紧跟潮流甚至略前沿**。#89 在 prefers-color-scheme 刚获浏览器初步支持时就落地自动深色模式，走在中文社区前面。

**心态**：从"补函数式/响应式认知"到"在真实项目交付中沉淀"。2020 元旦回顾坦承上半年停滞 4 个月、读书"难落地实践"，flag 明确（深入 React 源码）——直接预告下一阶段的爆发。

### 阶段 2｜2020 下–2021 中 [真]：从"用"到"懂"——响应式手写 + React 源码深挖巅峰

**作者职业生涯的技术深度顶点，也是 era_stance 最靠前的时期。**

两步走：（1）**预热（2020.10）**：Vue3 发布仅一月即拆解 Proxy 响应式，手写依赖收集、攻克 Map/Set 代理内部槽问题；（2）**巅峰（2021.02–06）**：紧贴 17.0.1 最新版的 React 源码全家桶——mount/双缓存 Fiber、Hooks、Effect、类组件、Context、useMemo/useCallback、EffectList、合成事件、Diff、props 更新、PureComponent/memo，再补 Vue3 reactivity 官方源码（[真]2021-06）。

**深度/增量变化**：**全面跃升至深度 4**。其中 Context 隐藏 API、props 更新机制达增量 4，挖出近乎无中文资料的实验性 API。这是从"API 用法文"到"源码字段级解释"的质变。

**相对当时潮流的位置**：**紧跟潮流偏超前**。Vue3 响应式（发布一月内）、React 17 源码（发布 4–6 月内通读）、隐藏实验 API 都属"走在中文社区源码解读前沿一档"。但需诚实区分：React 源码解读在 2021 初已是社区热门（卡颂源码课正热），作者是"紧跟潮流中较扎实的一档"，而非首发性原创研究。

**心态**：自驱、硬核、诚实。反复主动标注"没看懂""逻辑狗屁不通""可能不对"，不装懂。但暴露短板：多篇主动**回避 Lanes/优先级/调度**，且自封装 useDerivedState 出现当时即站不住的逻辑缺陷——深度虽到字段级，但对"为什么这样设计"的机制串联仍偏浅，停留在"带注释的源码走读"。

### 阶段 3｜2021 下–2022 初 [真]：工程化纵深 + 跨端转向

**关注点**：Yarn2 Monorepo（[真]2021-08，含自写 cjsb 打包工具）、前端路由/history 原理（[真]2021-12）、fastlane 双端自动化（[真]2021-11）、RN 原生模块 Swift/Kotlin（[真]2022-01）、Swift Concurrency（[真]2022-12）。

**深度/增量变化**：回落至深度 2–3/增量 3。多为实践总结，落地性强但原理解释偏少。

**相对当时潮流的位置**：分化——Yarn2（发布一年多、国内采用率低）= **紧跟偏前沿尝鲜者**；fastlane+match、RN 旧 Bridge = **主流跟随**；Swift Concurrency（发布约一年即系统整理）= **紧跟潮流**。

**心态**：从"啃源码"转向"解决跨端工程实际问题"，开始把 Swift/iOS 作为新能力边界探索。#127 把 await 误说成"阻塞"，暴露新领域概念尚未夯实。

### 阶段 4｜2022.12 – 2024.05：约 17 个月的博客空窗（详见下文停滞期解读）

### 阶段 5｜2024 中 [真]：回归 + App Router 复盘 + 强观点

**关注点**：Next.js App Router/RSC 博客重写（[真]2024-05）、shiki/twoslash/MDX 代码展示管线（[真]2024-06）、GitHub Discussions 当后端、Bento 设计。掘金时间线另记 2024.11 有"开发 1 年 App Router 后劝退文"（强观点复盘）。

**深度/增量变化**：深度 2/增量 1–2。技术栈前沿但内容偏选型罗列，缺实现细节（#132"全篇无代码示例，与自己主张相反"，这是当时即存在的内容缺陷）。

**相对当时潮流的位置**：**紧跟潮流**。App Router/RSC 稳定刚满一年即在生产推动并重写博客，属主流跟随；React Aria Components（刻意不选 shadcn/radix）、Discussions 当后端体现实践者品味与取舍。但从"源码深挖"退回到"选型与卖点"，深度未匹配选题前沿度。

**心态**：从纯学习者转为**有立场的实践者**。刻意的技术取舍、"开发 1 年后劝退"的强观点复盘，显示判断力成熟、不再盲从。

### 阶段 6｜2025 中 [真]：AI 辅助 + Swift 端侧音频

**关注点**：iOS 端用 ONNX Runtime + Core ML EP 跑 Silero VAD v5（[真]2025-07），代码由 Claude 生成。

**深度/增量变化**：深度 2/增量 3。填补 iOS Silero VAD 中文实践空白，状态机设计务实（双阈值抗抖动），但近乎纯代码堆砌、性能优化未实测验证。

**相对当时潮流的位置**：**紧跟潮流**。端侧语音/AI 辅助编码正是 2025 年中的典型做法。

**心态**：拥抱 AI 辅助、坦诚标注"代码由 Claude 生成"，工作流彻底改变；技术兴趣稳定锚定在 Swift/iOS 端侧 AI。

### 关键转折点

| # | 时间 | 转折 | 信号 |
|---|------|------|------|
| T1 | **2019 初** [真2019-04] | 工程化配置 → 主动追前沿（Apollo） | era_stance 首次从"主流跟随"升到"紧跟潮流" |
| T2 | **2020 上半年** [真] | "刷 API/读书" → 真实业务落地 | 应用类深度增量齐升到 3，元旦立"读 React 源码"flag |
| T3 | **2020.10 → 2021.02** [真] | **质变点**：Vue3 响应式手写预热 → React 17 源码长征 | 深度从 2–3 跃到 4，连续 14 篇源码解析，整条曲线巅峰 |
| T4 | **2021 下** [真2021-08] | 源码热退潮 → 工程化/跨端 | 从 Fiber 字段回到 Monorepo/fastlane/RN，落地性增强 |
| T5 | **2022.12 → 2024.05** | 约 17 个月博客空窗 | Swift Concurrency 后骤停，App Router 博客才恢复 |
| T6 | **2024 中** [真2024-05] | 学习者 → 有立场的实践者 | 刻意技术取舍 + "开发 1 年后劝退"强观点 |
| T7 | **2025** [真2025-07] | 引入 AI 辅助编码 | 坦承 Claude 生成代码，工作流转型 |

### 停滞期解读（约 17 个月空窗，**以下均为假说，非事实**）

博客连续产出止于 Swift Concurrency（[真]2022-12-25），下一篇 App Router 博客（[真]2024-05-21）相隔约 **17 个月**。需注意：GitHub discussions 导入日多为 2023-10-14，是导入日非写作日，无法据此填补空窗。

- **假说 A（最可能）—— 重心转移到工作落地，写作动机下降**：阶段 3 已显示从"啃源码炫技"转向"解决跨端实际问题"。**可能把精力投入公司 RN/iOS 项目交付，技术消化转为内部沉淀而非公开博客**。掘金线索"2024.11 开发 1 年 App Router"亦可反推 2023 大概率在做大量 App Router 实战，只是没写——**但需注意此为推断叠推断（该掘金线索本身的来源置信度未独立确证），应降权看待**。
- **假说 B —— 写作范式切换的过渡成本**：空窗后首篇即"把博客从 Astro 迁到 Next.js App Router"，**搭建新博客本身可能就是空窗的一部分**——旧博客方案维护疲劳，决定推倒重写，期间无发布渠道。与他多次重做博客的历史一致。
- **假说 C —— 技术疲劳/兴趣周期**：阶段 2 的源码长征强度极高，可能伴随"巅峰后倦怠"。

**反指标（说明并非"停止学习"）**：空窗结束后立刻产出 RSC、shiki/twoslash 等前沿内容，且有强观点复盘——说明**学习从未中断，只是公开写作中断**。最可能是 A+B 叠加。

### 趋势研判

**1. 能力轨迹：U 型深度曲线，技术栈持续下沉。** 深度走势 2(2019)→3(2020 应用)→**4(2021 源码巅峰)**→3–2(2022 跨端)→2(2024 博客)→2(2025)。深度回落**不代表能力退化**，而是关注点从"前端内部原理"转向"跨端/端侧/AI 等新边界"——每进新领域都从浅层重启，符合"广度扩张期"特征。**但需区分两类：'深度低'（新领域重启，可原谅）与'自相矛盾'（如博客重写篇'全篇无代码却主张代码展示管线'，属当时即存在的内容缺陷，不可全部归因于广度扩张而豁免）。**

**2. era_stance 总体稳定在"紧跟潮流"，偶有超前。** 横跨 7 年几乎从未滞后：每个新技术都在发布后数月内跟进。这是**一个长期保持技术敏感度、稳定走在主流前沿的开发者画像，绝非"jQuery 时代不用 React"式的滞后者**。需诚实区分"尝鲜"（Yarn2）与"随主流"（fastlane、RTL、App Router 本身），不宜全部拔高为"偏前"。

**3. 一以贯之的优点与短板。** 优点：诚实、真实业务驱动、覆盖面广、紧跟版本。短板：**"是什么/怎么用"强于"为什么这样设计"**——从 RxJS 跳过 scheduler、React 源码回避 Lanes、到 #137 不解释 Core ML EP 原理，深度常止于"现象/字段级"。这是贯穿 7 年最稳定的能力天花板。

**4. 研判结论。** 作者是典型的**高敏感度、广覆盖、诚实型实践者**。2021 React 源码期是其深度巅峰；此后转入广度扩张。若延续 2024–2025 轨迹，未来大概率在 **Swift/iOS + 端侧 AI/LLM 应用**方向继续深耕，并更多借助 AI 辅助。要再现 2021 式深度巅峰，需克服"重用法轻机制"的惯性。

---

## 五、内容评价：改进建议与批判

> 凡涉及"过时"的，一律不计入硬伤（自然折旧）。以下批评均经"非事后诸葛"检验。

### A 类·真问题（当时即错，会直接误导/坑读者）

按"读者照做会踩坑"的程度排序：

**A1. OAuth 架构选型错误 + 把 clientSecret/Token 注入前端（#118）。** 最危险的一条。`process.env.NEXT_PUBLIC_GITHUB_CLIENT_SECRET` 把密钥打进客户端 bundle，全文无任何安全提示。**且这不仅是"忘了加保护"，而是架构选型错误**——把 client_secret 放前端意味着整个 OAuth Web Application Flow 被用错了（secret 本就该只存在于后端换 token 的那一步）：即便不泄露，纯前端项目就不该用需要 secret 的 flow，应走 PKCE 或后端代理。读者照抄会把 secret 公开暴露。这在 2021 年同样错误——客户端可见密钥的风险与年份无关。**危害最大，因为它是"安全主题"文章里的安全漏洞。**

> 安全二次扫描（核查项，非定罪）：#103/#118 用 GitHub Issues/Discussions 当评论/数据后端，**若评论写入也依赖前端持有的 token，则存在任意用户可伪造/灌水/越权写入的风险**；若 token 仅在后端则不成立。建议作者自查这一面。

**A2. 自研方案的核心结论站不住 + 实现有缺陷（#114 useDerivedState）。** 两层问题：(a) 声称用 useRef 存 state 能"避免重渲染、比官方省一次执行"，但其 `setState` 仍调用 `rerender({})` 触发 useState 更新，函数组件照样重执行——"性能优势"前提不成立，"完美复现"是错误断言；(b) 判断用 `if (props?.value)` 而非比较前后值（官方的 `row !== prevRow`），导致每次 props 为真值都覆盖本地 state，且 0/''/false 等假值处理错误。这是"教读者一个错误的封装"，比单纯讲错 API 更坏。

**A3. 用 try/catch 兜底未知 type，吞掉真实运行时错误（#92）。** `messageCreatorMap[type]` 为 undefined 时靠 catch 兜底，会把策略函数内部真正的运行时错误也一并捕获并误报为"不支持的类型"，会掩盖线上 bug。当时正确做法是 `type in map` 或 `map[type] ?? fallback`。叠加同篇 `sendMessage`/`createMessage` 命名冲突、重复 `const sum`，示例直接不可运行。

**A4. 概念定义写反，示例却对——读者建立错误心智模型。**（**危害上调至此处：定义写反会被当结论记住、潜伏久且难自检，故排在"忘 restart"之前**）
- #65：Omit 文字定义写成"包含除 T 之外的 K 类型属性"（写反），Extract 解释成"从 T 提取包含 U 的属性"（语义反）。示例结果对，但定义错会被记住。
- #73：注释"过滤奇数"代码却保留偶数；"filter 接受第二个参数作默认值"（应为 first）。
- #69：`from({0:0,1:1,2:2,length:3})` 输出注释写成 `1 2 3`（应为 `0 1 2`）。

这类"定义/注释与代码矛盾"是速查型笔记的硬伤——读者查的就是结论，结论错了比不写更糟。

**A5. 阶段/概念命名错误（源码解析类，精度要求最高）。**
- #117：把 Placement 节点的 `insertBefore` 归到 Layout 阶段（应为 commit 的 mutation 阶段）；"key 把 O(n³) 降到 O(n)"归因不严谨（是整套启发式的功劳）。
- #110：`componentDidUpdate` 多处误写成 `componentDidMount`（标题 + 正文），代码却是对的。
- #127：把 `await` 描述为"阻塞"（实为协作式挂起/让出线程），是 Swift Concurrency 核心概念错误。
- #126：正文说 `DispatchQueue.main.async` 代码却是 `.sync`，主线程场景会死锁。

源码/底层解析类读者就是冲着精度来的，命名错误的杀伤力高于普通笔记。

**A6. 改配置只 start 不 restart（#66 trojan）。** 教程类最忌讳"照做卡住"。改完 config.json 只写 `start`，服务已在运行时不会生效，直接让读者卡在最后一步。

**A7. SIP 限制未提，恰好用 curl 举例（#79 proxychains）。** macOS 的 SIP 会阻止 DYLD 注入对 `/usr/bin/curl` 等受保护二进制生效，proxychains4 对这类命令静默失效。文中偏用 `curl ip.gs` 验证，读者必然遇到"代理不起作用"。**SIP 自 2015 年起即存在，写作当时已客观生效，属当时即错。**

**A8. 职责张冠李戴（#122、#76）。** 把 trigger 的职责算到 set 拦截；把 next 与 middlewareAPI.dispatch 混同。#76 尤其可惜——把"dispatch 占位 + 闭包延迟绑定"这个 applyMiddleware 最值得品的设计直接讲漏。

### 方法论/工程纪律问题（非过时，是习惯）

**M1. 止于"读源码复述"，不追问设计动机（最普遍）。** 反复出现"为什么这样设计"的留白：#77 的"为何逐层订阅而非直接订阅 store"（答案 zombie children 就在源码里，已逼近却没点破）、#105 列 5 个问题只给方向、#116/#119/#113 自定位"浅析"把最值钱的部分略过。这是 14 篇 React 源码系列的共同天花板。**（例外：#112 挖隐藏 API、#120 认知纠偏、#77 已逼近 zombie children，这几篇恰恰已触及 why，不在被否定之列。）**

**M2. 遇不懂直接跳过/留白，且常出现在最关键处。** #110 processUpdateQueue 优先级跳过"没看懂"（类组件并发更新最核心部分）、#111 `dependencies.firstContext = null` 标"没懂"、#75 保留 `isDispatching` 却因省略抛错使其成"死变量"。诚实标注值得肯定（比装懂好），但跳过的恰是难点，导致"深度卡在门口"。

**M3. 代码不自测/校对不严，笔误成灾。** `webpackChunksName`（#64）、`Mutetion`（#83）、`defaultEmpty`（#72）、全角逗号导致代码不可运行（#98）、`penderProps`（#120）、复制粘贴取错 key（#126）。源码/教程类对可运行性要求高，反复出现说明缺少"跑一遍再发"的纪律。

**M4. 选题同质化 + 个人增量偏低。** 增量度均值仅 2.40/5，4 分仅 3 篇、无 5 分。RxJS 7 篇、React 源码 14 篇，大量是"对照官方文档/参考书的忠实转述"（**原创撰写、非搬运；此处指个人增量有限**）。学习笔记可以转述，但常年缺少自己的踩坑、封装、对比实验。

### 价值、受众与天花板

**真实价值（不吹捧）：**
- **稀缺垂直实战是最高光部分**：#84（前端 SIP 软电话四种音频流废弃梯度）、#102（透明小组件各机型坐标手工测量）、#137（iOS Silero VAD Swift 封装）、#124（RN 双端 fastlane）。这些"网上几乎查不到"的一手踩坑，是 67 篇里增量最高、最不可替代的内容。
- **时效敏锐**：#77、#105/#106、#112、#127 多次踩在技术发布后数月的窗口期，走在中文社区前列。
- **诚实是贯穿优点**：几乎每篇都坦承能力边界、不装专家，是可信度的基石。

**受众定位：** 同阶段的进阶学习者（用了某技术、想知道原理的人），以及做相同冷门垂直场景（SIP/小组件/VAD/RN 打包）的开发者。**不适合**当权威参考——因笔误与定义错误较多。

**天花板（核心判断）：** 深度均值 2.52/5，无一篇 5 分。作者已具备"读最新源码 + 时效敏锐 + 工程落地 + 诚实"四项优点，**唯一卡住上限的是"分析停在 how，不深挖 why"**。#112（深度 3 增量 4）、#120（深度 4 增量 4）这类"挖隐藏 API + 认知纠偏"是天花板之作——共同点是都从"复述"跨到了"提炼出一个反直觉的结论"。若每篇源码解析都逼自己回答一个"为什么这样设计"，深度可整体上一个台阶。

### 可执行改进建议

1. **发布前跑一遍代码 + 过一遍注释（形式/可运行性验证）。** 直接消灭 A4（定义与示例矛盾）、M3（全角逗号、拼写、取错 key）。零成本、收益最大——这些都是当时就能发现的错误。
2. **安全红线：任何 token/secret 出现在前端代码时，强制加风险说明并区分服务端/客户端用法，且重新审视 flow 选型（#118）。** 客户端暴露密钥的风险与年份无关；纯前端应走 PKCE 或后端代理。
3. **自研封装必须配最小对照 demo + console 验证再下结论（语义/结论正确性验证，#114）。** "完美复现""有性能优势"这类断言，跑一个对照就能证伪。没验证就别下结论，或明确标注"未验证"。（**注：第 1 条验形式，第 2/3 条验语义，层级不同。**）
4. **源码解析每篇收尾回答一个"为什么这样设计"。** 把已逼近的答案点破：#77 的 zombie children、#76 的 dispatch 占位延迟绑定、#75 的 isDispatching 重入保护。这是从深度 2–3 迈向 4–5 的唯一杠杆。
5. **教程类补"验证闭环"与"失败前置条件"。** #66 改配置后必须 restart 并教读者确认生效；#80 禁 root 登录前先确认有可用非 root 密钥账户；#79 提示 SIP 对 curl 的限制。教程的价值 = 可复现。
6. **概念命名零容忍，尤其底层/并发主题。** #127 的 await≠阻塞、#117 的 mutation≠layout、#126 的 sync/async 死锁——核心概念错一个字，整篇可信度崩塌。
7. **把垂直实战做成可复现资产而非贴码堆。** #102 用表格 + 测量方法替代数百行坐标枚举；#137 讲清"为何 state 是 [2,1,128]、512 窗口的约定、Core ML EP 是否真加速（给实测数据）"。你最稀缺的内容，恰恰最值得讲透原理。
8. **降低同质化：每个学习系列至少留一篇"我踩的坑/我的封装/我做的对比实验"。** 挑一两篇深挖，胜过十篇平铺。

**一句话总结：** 优点（时效、诚实、垂直实战、敢读源码）已成型且稀缺；真正拖后腿的是"可避免的笔误"和"停在复述不深挖动机"。前者靠发布纪律，后者靠每篇多问一个"为什么"——两者都不需要更高的技术水位，只需要更狠一点的自我要求。

---

## 六、文笔与写作风格评级

**等级：B（中上 / 合格偏优的技术博主，非专业写作者）**

**清晰度：A-** 技术表达准确、降门槛能力突出。最强项是"以已知带未知"的类比策略：用 redux connect 引入高阶 Observable、用"河流汇聚"讲合并类操作符、用"圣诞树彩灯"比喻 EffectList、用 dio≈axios / setSize≈rem 给前端读者搭桥。源码解析中"裁剪源码 + 行内中文注释 + 段后白话复述"的范式信息密度高且好懂。如"`useState` 实际是一个自带了 `reducer` 的 `useReducer` 语法糖，所以需要放在一起分析"一句即把两个 API 的关系讲透。

**组织结构：B+** 结构意识清晰但模板化。源码解析普遍走"原理简述→源码浅析→总结"的稳定三段式，自顶向下按调用栈推进；实践类走"痛点→步骤→踩坑→收尾"线性流程。优点是可预期、好检索；短板是学习笔记多为"记到哪写到哪"，章节层级偶有不规整，部分文章无收束。

**可读性：A-** 节奏轻快、亲和力强，可操作性高（"在控制台输入 X，可以看到 Y"式引导），代码可直接复制运行。扣分项是校对粗糙：多篇有同音/手误错别字（"非常都简单""肯定是多的莫法"、Bable、aduit），属未精修的随手笔记质感。

**个性声音：A（最突出维度）** 极强的"小张"人设：自称"小张"、高频自嘲、坦诚暴露盲区（"// 没懂""我目前也没搞明白"），把学习笔记当日记写——结尾常飘到生活独白（macbook 烧焦、辞职待业、阳了发烧、想进大厂仰望云谦大佬）。这种真实、不端着的成长叙事是其最大辨识度，远超一般技术博客的"人味"。

**文采：C+** 不以辞藻见长，基本是大白话 + 网络黑话（"u1s1""蓝廋香菇""捞的淌口水"）。亮点在偶发的生动比喻和叠词夸张（"又双叒叕""最最最最简单"），但缺乏书面文采的锤炼，方言口语直出。

**综合：** 技术沟通力（清晰度/可读性）与个人声音达到 A 级业余博主上游水准，但组织规整度、文字精校、文采属合格线，故总评 B。定位准确——他写的是"真实学习记录"而非"精修技术文章"，且这份真实本身就是其价值。

---

## 七、写作风格复刻指南

### 声音与人设（一句话）
一个自称"小张"、坦诚谦逊又爱自嘲的前端工程师，把技术博客当学习笔记和生活日记混着写：技术上"边学边记、读源码、求成长进大厂"，语气上口语化、随性、真实不装懂。

### 开头模板（基于真实原句）
- **模板 A — 动机自述式（最常用）**：>"一直以来用的都是 `scss`，最近开发 Ant Design Pro 需要使用 `less`，记录学习笔记。" 套路：交代"最近在做什么/用到什么 → 所以记一篇笔记"，常带版本或环境说明。
- **模板 B — 情绪自白式**：>"最近学习很迷茫，深入学不懂，源码看不明白，迷茫之下，学一点其他的东西充实自己。" 套路：先暴露真实情绪或成长焦虑，再引出今天要学/要写的东西。
- **模板 C — 认知反差/痛点钩子式**：>"可以说在我打开源码的第一时间傻眼了。" / "面试官问我是如何浅对比的，结果我就没回答上来。" 套路：用"我本以为很简单→结果傻眼/被问倒"的反差或一次真实踩坑制造钩子。

### 文章结构骨架
**源码解析类（最典型）**：(1) 一句话交代范围 + 版本声明（"以下源码浅析的 React 版本为 17.0.1……不含优先级相关"）；(2) `原理简述`：用伪代码或极简示例讲直觉；(3) `源码浅析`：按调用栈/文件自顶向下，多级标题（## → ##### ），小标题直接用函数名命名；(4) 每节：一句白话概括职责 + 贴裁剪源码（保留 `// 省略...`）+ 行内中文注释；(5) 善用具体例子推演（环形链表 u0/u1/u2、abcd→acbd、value=-1/0/1）；(6) `总结`：文字复盘主线；(7) 谦逊收尾"如有错误，还望交流指正。"

**学习笔记类**：跟官方文档章节顺序平铺 → 每节"一句说明 + 代码块 + 输出注释" → 随性收尾（开放疑问/自嘲/生活独白），常无总结。

**实践总结类**：痛点/起因 → 步骤分小节（配截图/diff 代码块）→ 踩坑"现象→原因→方案"三段式 → 飘到生活吐槽收尾。

### 句子层面习惯
- **自称**："小张"（第三人称自指）、"我"交替使用。
- **口头禅/方言**："一把梭""咋个""莫法""打脑壳""u1s1""牛皮""服气服气""开搞开搞""蓝廋香菇"。
- **谦辞（高频）**："感觉这一篇很水""这样就水了一文""没啥营养含量""我也没大看懂""不知道对不对""把握不住"。
- **求助/留口子**："希望有大佬能解答""欢迎讨论""可能不一定对"。
- **成长叙事**："争取进大厂""云谦大佬是我的偶像""仔细回顾这一年的成长还是挺多的"。
- **连接词**："其实""不过""所以""然而""这里""可以看出""我的理解就是""用白话来看"。
- **中英混排**：技术名词一律用反引号包裹（`Observable`、`useState`、`infer`），中英之间不强制空格。
- **类比开路**："以已知带未知"——操作符=数组方法、合并=河流汇聚、EffectList=圣诞树彩灯、dio≈axios。
- **代码注释风格**：注释即讲解，直接写在代码块里（中文行内 `// ...`），讲不通时坦白写 `// 没懂`。
- **波浪号/emoji**：句尾爱加 `～`，情绪处用 😂🤦‍♂️💪☝️。
- **悬念省略**：用 `...` 三行制造"先想想再揭晓"。

### 排版规范
Markdown 多级标题切分知识点（源码解析可深到 `#####`）；行内技术名词、API、标识符全部用反引号；代码块承载主体，输出结果写成行内 `// 注释` 紧跟代码；配置改动用 `diff` 代码块（`+`/`-`）展示增量；书摘用 `>` blockquote；参考资料放结尾并给外链；引用偶用脚注上标 `<sup>`；配图佐证（截图/GIF）；用 `---` 分隔补充或被否定的旧内容；不追求精校，保持真实随手感。

### Do / Don't 清单
**Do**：开头交代动机和版本/环境；坦白知识盲区，不懂就写"没懂""还没看"；用熟悉概念类比陌生概念；代码裁剪 + 行内中文注释承担讲解；谦逊收尾、留讨论口子、附参考链接；穿插真实生活独白和成长焦虑；自称"小张"，适度自嘲。

**Don't**：不端着写"权威文档"腔，不堆术语炫技；不强行升华总结（很多篇戛然而止反而更像他）；不假装全懂、不掩饰借助 AI 或踩坑；不写代码注释之外的冗余原理长篇（除非确实在拆源码）；不追求排版完美，不必消灭所有口语和错别字；中文回复里别漏掉反引号包裹技术名词。

### 风格示例段落（模仿新写，约 150 字）
> 最近在新项目里用到 `Suspense`，之前一直觉得这东西很神秘，今天趁着摸鱼翻了下源码，记录一下，版本是 18.2.0。原理简述一下：其实就是抛出一个 `promise`，让上层的边界 catch 住再决定渲染 `fallback`，用白话说就是"组件没准备好就先举手喊一声"。我一开始以为会很复杂，结果打开源码……还真有点复杂哈哈。这里有一段我也没大看懂，蓝廋香菇，猜测和优先级调度相关。总之先水一篇，如有错误，还望大佬们交流指正～

---

## 八、数据附录

### 样本与统计
- **样本总量**：67 篇被成功分析（评判均以写作当时技术水位为基准）。另以掘金 39 篇真实日期做时间线校准。
- **领域分布（主要）**：React 源码 14、RxJS 7、CSS 6、博客工程 5、运维工具 4、Swift-iOS 4、构建工程化 3、React 应用 3、TypeScript 类型 2、状态管理 2、测试 2、随笔感悟 2，其余为 Redux 源码、GraphQL、WebRTC、Flutter、qiankun、JS 动画造轮子、响应式原理（Proxy + Map/Set）、Vue 源码、React Native、react-router/history 原理等各 1 篇。
- **类型分布**：源码解析 21、学习笔记 17、实践总结 17、读书笔记 4、教程指南 2、工具配置 2、随笔感悟 2、造轮子 1（含 1 篇源码解析/实践总结复合）。
- **技术深度（当时基准）**：均值 2.52/5。分布 1 分:5、2 分:34、3 分:16、4 分:12、5 分:0。
- **内容增量度（当时基准，与著作权无关，全部内容均为原创撰写）**：均值 2.40/5。分布 1 分:5、2 分:33、3 分:26、4 分:3、5 分:0。

### 【关键】当时即错 vs 自然折旧
- **当时即错条目（A 类真问题）：146 条，涉及 62/67 篇。** 这是真正可批评的部分，主要为命名冲突、未定义引用、概念定义写反、可运行性 bug、安全/架构错误等。
- **当时正确但如今过时条目（自然折旧）：198 条。** 这部分**一律不计作者过错**——它们在写作当时是恰当选择，仅因技术演进被淘汰（如 webpack4/CRA、RN 旧 Bridge、corejs:2、当时受限的 CSS 特性据此做的 fallback 等）。

> 对比说明：自然折旧条目（198）显著多于当时即错条目（146），这恰恰印证了作者"长期紧跟版本"的特征——大量内容是"当时前沿、随时代自然老化"，而非"当时就写错"。若以今天标准事后评判，极易把这 198 条误计为过错，本报告坚决不这样做。

### 方法与局限
- **GitHub 创建日期非真实发布日**：本次覆盖 67 篇 GitHub discussions，其中存在大量统一导入日（如 2023-10-14），是导入日而非写作日，**不能据此判断写作时间或填补空窗期**。真实时间线主要依据掘金 39 篇的发布日及正文内部的版本/事件锚点校准；无法锚定者标注为 [推]（据技术纪元推断）。
- **以当时为基准的评判方法论**：全程区分 (A) 当时即错与 (B) 自然折旧，难度/稀缺度计入正面证据，反对事后诸葛。个别"紧跟"经校准后修正为"随主流"（如 2021 中选 RTL 为合理跟随、非前瞻判断），以避免"用后见胜负给当时随大流贴金"的反向时代错置。
- **二手材料局限**：本报告基于逐篇摘要而非通读原文全文，存在二手转述的信息损耗；"增量度/深度"评分含主观成分；博客样本只能反映自学能力、源码钻研、技术嗅觉与表达，**无法覆盖系统设计、技术决策权衡、协作影响力、生产级工程质量**等维度，故综合定级置信度仅约 65%，真实职级可能因隐藏维度上浮或下沉。
- **著作权声明**：作者全部内容均为本人原创撰写，无搬运转载；本报告所称"增量低"仅指相对官方文档/参考书的个人增量有限，与原创性无关。
