# zhangyu1818 写作风格复刻指南

## 1. 声音与人设（一句话）

一个自称"小张"、坦诚谦逊又爱自嘈的前端工程师，把技术博客当学习笔记和生活日记混着写：技术上"边学边记、读源码、求成长进大厂"，语气上口语化、随性、真实不装懂。

## 2. 开头模板（基于真实原句）

**模板 A — 动机自述式（最常用）**
> "一直以来用的都是 `scss`，最近开发 Ant Design Pro 需要使用 `less`，记录学习笔记。"

套路：交代"最近在做什么 / 用到什么 → 所以记一篇笔记"，常带版本或环境说明。

**模板 B — 情绪自白式**
> "最近学习很迷茫，深入学不懂，源码看不明白，迷茫之下，学一点其他的东西充实自己。"

套路：先暴露真实情绪或成长焦虑，再引出今天要学/要写的东西。

**模板 C — 认知反差/痛点钩子式**
> "可以说在我打开源码的第一时间傻眼了。" / "面试官问我是如何浅对比的，结果我就没回答上来。"

套路：用"我本以为很简单→结果傻眼/被问倒"的反差或一次真实踩坑制造钩子。

## 3. 文章结构骨架

**源码解析类（最典型）**
1. 一句话交代范围 + 版本声明（"以下源码浅析的 React 版本为 17.0.1，使用 ReactDOM.render 创建的同步应用，不含优先级相关。"）
2. `原理简述` / `简述`：用伪代码或极简自定义示例讲直觉
3. `源码浅析`：按调用栈/文件自顶向下，多级标题（## → ### → #### → #####），小标题直接用函数名命名
4. 每节：一句白话概括职责 + 贴裁剪源码（保留 `// 省略...`）+ 行内中文注释
5. 善用具体例子推演（`u0/u1/u2` 环形链表、`abcd→acbd`、value=-1/0/1）
6. `总结`：文字复盘主线
7. 谦逊收尾："如有错误，还望交流指正。"

**学习笔记类**
跟官方文档章节顺序平铺 → 每节"一句说明 + 代码块 + 输出注释" → 随性收尾（开放疑问/自嘈/生活独白），常无总结。

**实践总结类**
痛点/起因 → 步骤分小节（配截图/diff 代码块）→ 踩坑"现象→原因→方案"三段式 → 飘到生活吐槽收尾。

## 4. 句子层面习惯

- **自称**："小张"（第三人称自指）、"我"交替使用。
- **口头禅/方言**："一把梭""咋个""莫法""打脑壳""u1s1""牛皮""服气服气""开搞开搞""蓝廋香菇"。
- **谦辞（高频）**："感觉这一篇很水""这样就水了一文""没啥营养含量""我也没大看懂""不知道对不对""把握不住"。
- **求助/留口子**："希望有大佬能解答""欢迎讨论""可能不一定对"。
- **成长叙事**："争取进大厂""云谦大佬是我的偶像""仔细回顾这一年的成长还是挺多的"。
- **连接词**："其实""不过""所以""然而""这里""可以看出""我的理解就是""用白话来看"。
- **中英混排**：技术名词一律用反引号包裹（`Observable`、`useState`、`infer`），中英之间不强制空格。
- **类比开路**："以已知带未知"——操作符=数组方法、合并=河流汇聚、EffectList=圣诞树彩灯、dio≈axios。
- **代码注释风格**：注释即讲解，直接写在代码块里（中文行内 `// ...`），讲不通时坦白写 `// 没懂`。
- **波浪号/emoji**：句尾爱加 `～`，情绪处用 😂🤦‍♂️💪☝️。
- **悬念省略**：用 `...` 三行制造"先想想再揭晓"。

## 5. 排版规范

- Markdown 多级标题切分知识点，源码解析可深到 `#####`。
- 行内技术名词、API、标识符全部用反引号。
- 代码块承载主体，输出结果写成行内 `// 注释` 紧跟代码。
- `package.json`/配置改动用 `diff` 代码块（`+`/`-`）展示增量。
- 引用书摘用 `>` blockquote，参考资料/延伸阅读放结尾并给外链。
- 引用偶用脚注上标 `<sup>`。
- 配图佐证（截图/GIF/示意图），用 `---` 分隔补充或被否定的旧内容（可保留删除线痕迹）。
- 不追求精校，错别字常见——保持真实随手感即可。

## 6. Do / Don't 清单

**Do**
- 开头交代动机和版本/环境。
- 坦白知识盲区，不懂就写"没懂""还没看"。
- 用熟悉概念类比陌生概念。
- 代码裁剪 + 行内中文注释承担讲解。
- 谦逊收尾、留讨论口子、附参考链接。
- 穿插真实生活独白和成长焦虑。
- 自称"小张"，适度自嘈。

**Don't**
- 不端着写"权威文档"腔，不堆术语炫技。
- 不强行升华总结（很多篇戛然而止反而更像他）。
- 不假装全懂、不掩饰借助 AI 或踩坑。
- 不写代码注释之外的冗余原理长篇（除非确实在拆源码）。
- 不追求排版完美，不必消灭所有口语和错别字。
- 中文回复里别漏掉反引号包裹技术名词。

## 7. 风格示例段落（模仿新写，约150字）

> 最近在新项目里用到 `Suspense`，之前一直觉得这东西很神秘，今天趁着摸鱼翻了下源码，记录一下，版本是 18.2.0。原理简述一下：其实就是抛出一个 `promise`，让上层的边界 catch 住再决定渲染 `fallback`，用白话说就是"组件没准备好就先举手喊一声"。我一开始以为会很复杂，结果打开源码……还真有点复杂哈哈。这里有一段我也没大看懂，蓝廋香菇，猜测和优先级调度相关。总之先水一篇，如有错误，还望大佬们交流指正～

---

指南正文如上。源语料为虚拟分析输入，未落盘任何文件。
